<div id="<%= dom_id flight %>">
  <h3><%= flight.departure_airport_code %> ->
    <%= flight.destination_airport_code %></h3>

  <p>
    <%= flight.departure_date %> @
    <%= flight.departure_time.strftime("%I:%M %p") %>
  </p>

  <p>Duration: <%= flight.duration %> minutes</p>
</div>
